# ── Agent Global Settings ─────────────────────────────────────────────────────
agent:
  mode: paper                         # paper | live
  primary_exchange: blofin
  log_level: INFO
  heartbeat_interval: 5               # seconds

# ── Trading Universe ─────────────────────────────────────────────────────────
trading:
  symbols:
    - BTC-USDC
    - ETH-USDC
    - SOL-USDC
    - XRP-USDC
    - DOGE-USDC
    - BNB-USDC
    - SUI-USDC
    - ENA-USDC
    - LINK-USDC
    - ADA-USDC
  market_types:
    - futures                         # futures | spot
  default_leverage: 5
  max_leverage: 20
  timeframes:
    - 1m
    - 3m
    - 5m
    - 15m
    - 1h
    - 4h
    - 1d
  primary_timeframe: 15m

# ── Signal Engine ─────────────────────────────────────────────────────────────
signals:
  min_confidence_threshold: 25        # minimum ensemble confidence to generate a signal
  confluence_required: 3              # all 3 subscribed TFs (15m+1h+4h) must agree
  lookback_candles: 500               # candles fed into ML models

# ── Risk Manager ─────────────────────────────────────────────────────────────
risk:
  max_portfolio_risk_pct: 2.0         # max % of portfolio at risk at any time
  max_single_trade_risk_pct: 0.5      # max % risked per trade
  max_daily_drawdown_pct: 5.0         # circuit breaker: halt if exceeded
  max_weekly_drawdown_pct: 10.0
  kelly_fraction: 0.25                # fractional Kelly (conservative)
  min_liquidation_distance_atr: 3.0   # entry must be 3x ATR from liquidation
  max_open_positions: 10
  max_correlated_positions: 3         # max positions in correlated assets (raised for 10-symbol universe)
  atr_sl_multiplier: 2.0              # stop = entry ± N*ATR — optimized via grid search (BTC OOS Sharpe 6.50)
  rr_ratio: 2.0                       # reward:risk ratio for take-profit
  adx_min_threshold: 20.0             # minimum ADX to confirm trend — blocks ranging market entries
  trailing_stop_activation_atr: 0     # 0 = disabled (trail incompatible with 2.5 ATR SL / 2:1 RR)
  trailing_stop_distance_atr: 1.5     # trail N*ATR behind the best price seen

# ── Execution ─────────────────────────────────────────────────────────────────
execution:
  default_order_type: limit           # limit | market
  limit_order_offset_pct: 0.05        # how far inside spread to place limit
  max_slippage_pct: 0.1               # cancel if expected slippage exceeds this
  order_timeout_seconds: 30           # cancel unfilled limit orders after N sec
  retry_attempts: 3
  retry_backoff_seconds: 2

# ── Paper Trading ─────────────────────────────────────────────────────────────
paper:
  starting_balance: 10000             # USDT
  simulated_fee_pct: 0.05             # maker fee
  simulated_slippage_pct: 0.02

# ── Sentiment & Intelligence ─────────────────────────────────────────────────
sentiment:
  enabled: true
  refresh_interval_seconds: 300        # 5 minutes
  
  # Fear & Greed Index
  fear_greed_enabled: true
  fear_greed_weight: 0.35
  
  # News aggregation
  news_enabled: true
  news_sources:                      # priority order
    - cryptocompare
    - coingecko
  news_weight: 0.25
  
  # Twitter/X sentiment (disabled - API now requires paid subscription)
  twitter_enabled: false
  twitter_weight: 0.0
  
  # Reddit sentiment (disabled - requires API approval)
  reddit_enabled: false
  reddit_weight: 0.0
  
  # Google Trends
  google_trends_enabled: true
  google_trends_weight: 0.10

# ── On-Chain Data ─────────────────────────────────────────────────────────────
onchain:
  enabled: true
  refresh_interval_seconds: 300        # 5 minutes
  
  # Cross-exchange funding rates (free APIs)
  funding_enabled: true
  funding_weight: 0.30
  
  # Open Interest (free from Binance)
  oi_enabled: true
  oi_weight: 0.25
  
  # Liquidations (estimated from price moves)
  liquidations_enabled: true
  liquidations_weight: 0.25
  
  # BTC Dominance (free from CoinGecko)
  dominance_enabled: true
  dominance_weight: 0.20
  
  # Whale Alerts (disabled - requires paid API)
  whales_enabled: false
  whale_min_value_usd: 100000

# ── Signal Weights ───────────────────────────────────────────────────────────
ensemble_weights:
  technical: 0.40   # raised: ML was overriding TA when biased SHORT in uptrend
  structure: 0.30   # raised: pure price-action shouldn't be overridden by stale model
  ml: 0.10          # reduced from 0.25 — models are period-biased; retrain weekly
  volume: 0.10
  sentiment: 0.05
  onchain: 0.05

# ── Per-Symbol Overrides ─────────────────────────────────────────────────────
# Based on grid search (3000 bars, 25% OOS split) — Mar 02 2026
# 15m thresholds applied globally per symbol (primary ML timeframe)
# 3m/5m OOS highlights noted in comments for reference
symbol_overrides:
  # ── Original 5 (15m grid search Mar 01) ──────────────────────────────────
  BTC-USDC:  {min_confidence_threshold: 25, atr_sl_multiplier: 2.0}  # 15m Sharpe=6.50 WR=55% | 5m:conf=50 atr=1.5 Sharpe=14.35
  ETH-USDC:  {min_confidence_threshold: 40, atr_sl_multiplier: 2.0}  # 15m Sharpe=9.16 WR=75% | 5m:conf=40 atr=2.0 Sharpe=14.57
  SOL-USDC:  {min_confidence_threshold: 30, atr_sl_multiplier: 2.0}  # 15m Sharpe=9.22 WR=62% | 5m:conf=30 atr=2.0 Sharpe=11.17
  XRP-USDC:  {min_confidence_threshold: 55, atr_sl_multiplier: 2.0}  # 15m: no good config  | 5m:conf=30 atr=2.0 Sharpe=11.04
  DOGE-USDC: {min_confidence_threshold: 25, atr_sl_multiplier: 2.0}  # 15m Sharpe=6.84 WR=58% | 5m:conf=25 atr=1.5 Sharpe=3.47
  # ── New 5 (15m grid search Mar 02) ───────────────────────────────────────
  BNB-USDC:  {min_confidence_threshold: 25, atr_sl_multiplier: 2.0}  # 15m Sharpe=6.54 WR=50% | 5m:conf=45 atr=1.5 Sharpe=7.61
  SUI-USDC:  {min_confidence_threshold: 35, atr_sl_multiplier: 2.5}  # 15m Sharpe=4.27 WR=67% | 5m:conf=40 atr=2.0 Sharpe=11.82
  ENA-USDC:  {min_confidence_threshold: 25, atr_sl_multiplier: 1.5}  # 15m Sharpe=11.3 WR=73% | 5m:conf=35 atr=2.0 Sharpe=10.02
  LINK-USDC: {min_confidence_threshold: 25, atr_sl_multiplier: 3.0}  # 15m Sharpe=6.81 WR=64% | 5m:conf=45 atr=2.0 Sharpe=2.64
  ADA-USDC:  {min_confidence_threshold: 30, atr_sl_multiplier: 2.0}  # 15m Sharpe=16.39 WR=67% | 5m:conf=40 atr=2.0 Sharpe=9.92
